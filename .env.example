# MySQL connection string used by Prisma
DATABASE_URL="mysql://appuser:apppassword@localhost:3306/auth_app"

# Secret used to sign session JWTs. Generate with: openssl rand -base64 32
SESSION_SECRET="change-me"
